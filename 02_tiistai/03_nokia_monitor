#!/usr/bin/env python3
# ==============================================================================
# Lab 01: Sisu System Monitor (Nokia Legacy)
# Target File: nokia_monitor.py
# Topics: subprocess.run, Popen, Process Pipelines, Exit Codes, Timeout Handling
# ==============================================================================

import subprocess
import sys
from typing import Tuple


# ==============================================================================
# TODO 1: Simple Command Execution
# ==============================================================================
def get_kernel_info() -> Tuple[str, int]:
    """
    Executes 'uname -a' using subprocess.run().

    Tasks:
    1. Run 'uname -a' with stdout and stderr captured as text strings (text=True).
    2. Check the command exit code.
    3. Return a tuple containing (stdout_string.strip(), exit_code).
    """
    # TODO 1: Implement kernel info capture using subprocess.run
    raise NotImplementedError("TODO 1 is not implemented.")


# ==============================================================================
# TODO 2: Error Handling & Exit Codes
# ==============================================================================
def trigger_system_error() -> str:
    """
    Attempts to list a non-existent directory (e.g., 'ls /invalid_sisu_path').

    Tasks:
    1. Call subprocess.run() with check=True to force a CalledProcessError.
    2. Catch subprocess.CalledProcessError in a try/except block.
    3. Extract the stderr output from the caught exception object and return it.
    """
    # TODO 2: Implement error trapping with check=True and try/except
    raise NotImplementedError("TODO 2 is not implemented.")


# ==============================================================================
# TODO 3: Subprocess Pipeline (Simulating 'ps aux | grep python')
# ==============================================================================
def find_running_python_processes() -> str:
    """
    Recreates a multi-process pipeline WITHOUT using 'shell=True'.
    Pipeline equivalent: ps aux | grep python

    Tasks:
    1. Spawn p1 = subprocess.Popen(['ps', 'aux'], stdout=subprocess.PIPE).
    2. Spawn p2 = subprocess.Popen(['grep', 'python'], stdin=p1.stdout, stdout=subprocess.PIPE, text=True).
    3. Close p1.stdout in Python to allow p1 to receive SIGPIPE if p2 exits early.
    4. Call p2.communicate() to retrieve the filtered output string and return it.
    """
    # TODO 3: Implement process-to-process piping using Popen
    raise NotImplementedError("TODO 3 is not implemented.")


# ==============================================================================
# TODO 4: Sisu Stress Test (Timeout Management)
# ==============================================================================
def run_stress_test() -> bool:
    """
    Simulates a long-running diagnostic process that hangs or times out.

    Tasks:
    1. Attempt to run 'sleep 10' with a strict timeout parameter of 2 seconds.
    2. Catch subprocess.TimeoutExpired.
    3. Print a warning to sys.stderr: "[!] Diagnostic process timed out (SIGKILL)."
    4. Return True if the process successfully timed out as intended, else False.
    """
    # TODO 4: Implement timeout management with subprocess.TimeoutExpired
    raise NotImplementedError("TODO 4 is not implemented.")


# ==============================================================================
# MAIN EXECUTION FLOW
# ==============================================================================
def main():
    print("==================================================")
    print("      SISU SYSTEM MONITOR DIAGNOSTIC SUITE        ")
    print("==================================================")

    # Test TODO 1
    try:
        kernel_info, code = get_kernel_info()
        print(f"\n[+] TODO 1 - Kernel Info (Exit Code {code}):")
        print(f"    {kernel_info}")
    except NotImplementedError:
        print("\n[-] TODO 1: Not Implemented")

    # Test TODO 2
    try:
        err_msg = trigger_system_error()
        print("\n[+] TODO 2 - Captured Stderr from System Failure:")
        print(f"    {err_msg.strip()}")
    except NotImplementedError:
        print("\n[-] TODO 2: Not Implemented")

    # Test TODO 3
    try:
        proc_list = find_running_python_processes()
        print("\n[+] TODO 3 - Active Python Processes:")
        for line in proc_list.strip().split("\n"):
            print(f"    {line}")
    except NotImplementedError:
        print("\n[-] TODO 3: Not Implemented")

    # Test TODO 4
    try:
        timed_out = run_stress_test()
        if timed_out:
            print("\n[+] TODO 4 - Timeout Handler Functioned Correctly.")
    except NotImplementedError:
        print("\n[-] TODO 4: Not Implemented")

    print("\n==================================================")


if __name__ == "__main__":
    main()
